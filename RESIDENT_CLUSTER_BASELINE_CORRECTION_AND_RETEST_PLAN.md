# 常驻集群基线纠正与重新验证执行规划

## 1. 状态与执行边界

本文只规划下一轮操作。用户明确同意前，不修改集群、部署包、源码或已有两份文档，也不提交或推送。

本轮目标是先审计并纠正三套调度方案的性能基线，再以“场景 1”作为最小测试；最小测试最多执行两轮、最多进行一次失败后的修复。任意一轮场景 1 通过后，只执行一轮完整测试。完整测试无论成功或失败，都只记录、分析和恢复，不进行完整测试后的修复或重跑。

执行分支：

```text
三套调度方案基线审计与按需纠正
  -> 更新记录并创建、推送首次纠正 Commit
  -> 调用一轮 gpt-5.6-sol / ultra 子 Agent 评审
  -> 将评审追加到代码评审文件，由主 Agent 判断和按需修订
  -> 提交、推送评审记录及必要修订
  -> 服务器拉取评审处理后的最终 Commit 并核对基线
  -> 场景 1（第 1 轮）
       |-- 通过 -> 完整测试（仅 1 轮）-> 更新报告 -> 结束
       `-- 失败 -> 归因 -> 追加修复方案 -> 修复并提交、推送
                    -> 服务器拉取新提交
                    -> 场景 1（第 2 轮）
                         |-- 通过 -> 完整测试（仅 1 轮）-> 更新报告 -> 结束
                         `-- 失败 -> 更新报告并恢复集群 -> 结束
```

## 2. 基线审计范围

基线审计不只检查 Kueue，而是检查三套调度方案及其直接依赖的全部调度组件：

### 2.1 Kueue + Coscheduling

- `kueue-controller-manager`
- `coscheduling`
- `scheduler-plugins-controller`
- Kueue API client QPS、Burst
- Kueue 各 Controller 并发参数
- Coscheduling parallelism、QPS、Burst 和插件配置

### 2.2 Volcano

- `volcano-scheduler`
- `volcano-controllers`
- `volcano-admission`
- Scheduler actions 和插件配置
- 与吞吐相关的客户端、并发及资源参数

### 2.3 YuniKorn

- `yunikorn-scheduler`
- `yunikorn-admission-controller`
- Scheduler 模式、队列和 Admission 配置
- 与吞吐相关的客户端、并发及资源参数

### 2.4 比较依据

逐项比较：

1. 常驻集群改造前提交 `6ce46e0cd2464a5c03331f8ee756980719ca4d69` 中的临时集群配置。
2. 当前常驻集群部署包中的期望配置。
3. 当前集群实际生效配置。
4. 已批准的版本兼容性和隔离要求。

重点检查：

- CPU request/limit
- memory request/limit
- QPS、Burst、parallelism 和 Controller 并发
- 调度插件、队列、Webhook/Admission 作用域
- 配置文件与 Deployment 实际值是否一致

## 3. 基线纠正原则

### 3.1 保留已经批准的变化

以下内容不因本次纠正回退：

- Kubernetes `v1.34.8`
- 1000 个 KWOK Node
- Volcano `v1.15.1`
- Kueue `v0.19.0`
- Scheduler Plugins `v0.34.7`
- YuniKorn `v1.9.0`
- 三个专用测试命名空间和 Webhook/Admission 隔离
- 常驻 Prometheus、Grafana、Image Renderer 和 Audit Exporter
- 常驻集群串行实验模型

版本不降级；旧版本字段只在当前版本仍支持、含义一致且确实影响基线时才迁移。

### 3.2 修正未经确认的性能参数变化

旧临时集群对调度组件的共同资源基线是：

```text
CPU request: 500m
CPU limit:   8
memory request/limit: 未设置
```

当前常驻集群中，Kueue、Volcano、Coscheduling 和 YuniKorn 的多个 Deployment 被设置了不同的 CPU 值和 memory limit。执行时逐项确认；没有当前版本的必要性或用户批准依据时，恢复到旧资源基线。

Kueue 还需检查并按当前版本兼容性恢复旧性能基线：

```text
clientConnection.qps:   1000
clientConnection.burst: 1000
当前版本仍启用且名称兼容的 Controller 并发: 100
```

不把旧版本中未启用、已删除或语义变化的字段机械写入新版本。Volcano、Coscheduling 和 YuniKorn 也按同样规则审计其吞吐与并发参数；发现需要修正时，与 Kueue 一并处理，不能等测试失败后才检查。

### 3.3 不保存或恢复当前不稳定基线

不备份当前 Deployment、ConfigMap 或安装文件，也不为当前 512 MiB 等未确认状态设计回滚流程。

执行前只做必要的只读核对并在报告中记录关键旧值；随后直接更新本地部署包、远端部署包和实时集群。这样不会把当前不稳定配置继续保留为所谓“可恢复基线”。

## 4. 首次基线纠正步骤

### 4.1 执行前清理

- 确认没有实验进程运行。
- 执行 `make down`。
- 确认 `.resident-state/` 不存在。
- 确认 Kueue、Volcano、YuniKorn 测试资源零残留。
- 确认 1001/1001 Node Ready，调度器和监控处于当前空闲状态。

### 4.2 追加文档二

在 [RESIDENT_CLUSTER_FULL_TEST_REPORT.md](./RESIDENT_CLUSTER_FULL_TEST_REPORT.md) 后追加新章节，保留第一次完整测试原文，不覆盖历史结论。追加内容包括：

- 旧基线、当前部署包和当前实际配置的对比表
- 哪些差异是已经批准的版本/隔离变化
- 哪些差异属于未经确认的性能基线变化
- 三套调度方案各自是否需要修正
- 最终采用的修正值和理由
- 本轮测试分支、轮次上限和停止条件

首次基线纠正不修改 [RESIDENT_CLUSTER_SOURCE_CHANGE_PLAN.md](./RESIDENT_CLUSTER_SOURCE_CHANGE_PLAN.md)，因为这是集群基线纠正，不是常驻集群源码设计变化。

### 4.3 修改部署包与集群

- 修改本地部署包中对应的 values、manifest、配置文件和安装脚本。
- 将相同修改同步到 `/root/benchmark-1348-deploy`。
- 应用三套调度方案中经审计确认需要修正的部分。
- 等待所有被更新组件重新 Ready。
- 校验 Deployment 实际资源、ConfigMap 内容和启动日志。
- 重新运行基础集群、调度器和监控验证脚本。

更新实际部署记录：

- [CLUSTER_DEPLOYMENT_RECORD.md](./CLUSTER_DEPLOYMENT_RECORD.md)
- `.codex/AGENT.md`

只记录最终生效基线，不保留当前不稳定状态作为恢复目标。

### 4.4 首次纠正 Commit

完成基线纠正和本地记录后，必须在场景 1 第 1 轮开始前：

1. 检查仓库变更只包含本轮批准内容。
2. 创建本轮基线纠正 Commit；Commit 至少包含文档二和集群部署记录，禁止为了形成 Commit 制造无意义源码改动。
3. 推送到远端 Git。

首次纠正 Commit 是后续独立评审的固定对象；评审期间不继续执行测试。

### 4.5 独立子 Agent 配置评审

首次纠正 Commit 推送后，调用且只调用一轮子 Agent：

```text
model: gpt-5.6-sol
reasoning effort: ultra
```

评审对象：

- 首次纠正 Commit
- 改造前提交 `6ce46e0cd2464a5c03331f8ee756980719ca4d69`
- 本地和远端部署包中的最终期望配置
- 当前集群实际生效配置
- 文档一、文档二和集群部署记录

评审必须覆盖 Kueue/Coscheduling、Volcano 和 YuniKorn，重点判断：

1. 资源、QPS、Burst、parallelism、Controller 并发、插件和队列等性能配置是否与旧基线一致。
2. 不一致项是否有当前版本兼容性、隔离或已批准设计依据。
3. 是否存在把旧版本字段机械复制到新版本、但字段已经删除、改名、语义变化、控制器未启用或不再适用的问题。
4. 是否错误回退了已批准的 Kubernetes、调度器版本、命名空间或 Webhook/Admission 隔离设计。
5. 部署包、集群实际值和记录文件是否互相一致。

子 Agent 不修改部署包、集群或其他源码，只把评审结果作为一个带被审 Commit 哈希的新章节，追加到：

[RESIDENT_CLUSTER_CODE_REVIEW.md](./RESIDENT_CLUSTER_CODE_REVIEW.md)

原有评审内容保留，不覆盖。

### 4.6 主 Agent 判断、按需修订与最终同步

主 Agent 收到评审后逐项判断：

- 接受的问题：给出原因，修订本地部署包、远端部署包、实时集群及相关记录，并完成健康检查。
- 不接受的问题：在评审文件中记录拒绝理由和依据，不做对应修改。
- 如果修订涉及常驻集群源码设计，按需更新文档一和 `.codex/AGENT.md`；仅涉及基线纠正时不修改文档一。

全部评审项的接受、拒绝及修订结果都追加到同一个评审章节之后，保证评审意见和最终处理可以对应追溯。

无论是否需要配置修订，评审结果已经修改评审文件，因此都必须再创建一个 Commit：

- 有修订：该 Commit 同时包含评审记录、主 Agent 结论和必要修订。
- 无修订：该 Commit 包含评审记录和主 Agent 的“无需修订”结论。

随后：

1. 推送评审处理后的最终 Commit。
2. 服务器仓库拉取并切换到该最终 Commit。
3. 核对本地、远端 Git 和服务器 HEAD 一致。
4. 核对服务器实时集群与修正后的部署包一致。

本阶段只调用一轮评审子 Agent；如果主 Agent 根据该评审进行了修订，不再自动发起第二轮子 Agent 复审。

只有 Commit、部署包和实时集群三者一致后，才允许执行场景 1。

## 5. 场景 1 最小测试：第 1 轮

执行：

```bash
make serial-test \
  TEST_TIMEOUT_SECONDS=350 \
  NODES_SIZE=1000 \
  QUEUES_SIZE=1 \
  JOBS_SIZE_PER_QUEUE=10000 \
  PODS_SIZE_PER_JOB=1
```

这是完整的场景 1，必须依次覆盖 Kueue、Volcano 和 YuniKorn，不是只测试 Kueue。

执行期间记录：

- 被测 Commit、部署包关键文件摘要和开始/结束时间
- 每套调度器执行结果和耗时
- 各调度核心组件内存峰值、OOM、重启和 Ready 状态
- Job、Pod、Workload/PodGroup 等对象创建与回收情况
- Audit Exporter 与 Prometheus 抓取状态
- 结果目录、三份审计日志和八张 Grafana PNG
- 测试结束后的资源残留和集群恢复状态

通过条件：

- Kueue、Volcano、YuniKorn 三轮全部成功。
- 所有目标组件没有 OOM、CrashLoop 或异常重启。
- 结果目录、三份审计日志、八张 PNG 和结果时间窗有效。
- 三套测试资源及 resident state 零残留。
- `make down` 后集群恢复到纠正后的基线。

源码现有 `serial-test` 不会可靠地在子阶段失败时停止，因此执行时监控每个子阶段。确认失败后停止后续无效阶段、保留证据并恢复集群，不能只依据最外层返回码判断成功。

## 6. 第 1 轮失败后的唯一一次修复

如果场景 1 第 1 轮失败，先追加文档二，再进行任何修复。

### 6.1 归因顺序

分析必须按以下顺序：

1. 判断是否由未经批准或偏离方案的源码改动造成。
2. 判断是否仍存在未经批准的集群性能基线变化。
3. 判断是否为部署包与实时集群不一致。
4. 判断是否为当前组件版本行为、资源容量、超时或测试模型本身的问题。

文档二记录：

- 本轮实验结果和现场证据
- 失败命令、直接原因和根因判断
- 与改造前源码的差异核对
- 归因结论及证据强度
- 唯一一次后续修复的具体方案

### 6.2 文档更新边界

- 仅修正集群基线：不修改文档一；更新文档二、部署记录和 `.codex/AGENT.md`。
- 修正源码但不改变已批准设计：更新文档二，文档一按实际需要补充。
- 修正涉及常驻集群设计变化：必须同步更新文档一和 `.codex/AGENT.md`。

### 6.3 修复、提交和同步

- 实施唯一一次修复。
- 完成本地静态检查和与风险相称的验证。
- 更新文档二及其他按需文档。
- 创建新 Commit 并推送。
- 服务器拉取该 Commit，核对 HEAD。
- 如果涉及部署包或集群配置，同步部署包、应用实时集群并完成健康检查。

完成以上步骤后，才执行场景 1 第 2 轮。

## 7. 场景 1 最小测试：第 2 轮

第 2 轮使用与第 1 轮完全相同的命令、参数和验收标准。

- 通过：进入一轮完整测试。
- 失败：不再进行第三次修复或第三轮场景 1；保存现场、执行 `make down`、完成集群健康检查，把第二轮结果和最终分析追加到文档二，然后停止。

因此本轮流程最多包含：

- 两轮场景 1
- 一次场景 1 失败后的修复

## 8. 新一轮完整测试

任一轮场景 1 通过后，执行一次：

```bash
make
```

完整测试只执行一轮。执行时逐场景、逐调度器监控真实子阶段结果；出现确认失败时停止后续无效执行并保留现场，不能让分号串行掩盖失败。

无论成功还是失败：

1. 记录八个场景中实际执行到的每个调度器、耗时和结果。
2. 记录结果目录、审计日志、Grafana 图片和 Prometheus 时间窗。
3. 记录失败命令、组件状态、资源峰值和残留资源。
4. 执行 `make down` 并恢复纠正后的固定集群基线。
5. 验证 1001/1001 Node Ready、调度组件、配置、监控和零残留。
6. 将结果和分析追加到文档二。

完整测试失败后不修复、不重跑。

## 9. 文档、提交与最终产物

### 9.1 必须维护的记录

- 文档二：保存基线审计、每次修复、两轮以内的场景 1、完整测试及最终分析。
- `RESIDENT_CLUSTER_CODE_REVIEW.md`：追加首次纠正 Commit 的独立配置评审和主 Agent 处理结论。
- `CLUSTER_DEPLOYMENT_RECORD.md`：记录最终实际部署值和安装方式。
- `.codex/AGENT.md`：记录当前集群与调度组件最终配置。
- 文档一：首次基线纠正不修改；只有后续源码设计确实变化时才按需修改。

### 9.2 提交规则

- 首轮场景 1 前必须完成首次基线纠正 Commit 和推送、一次 `gpt-5.6-sol / ultra` 独立评审、评审处理 Commit 和推送，以及服务器对最终 Commit 的拉取。
- 第二轮场景 1 前如发生修复，必须完成新的 Commit、推送和服务器拉取。
- 流程结束后，把最终文档二和部署记录做收尾 Commit 并推送；该提交不再触发新的测试。
- 每次 Commit 前检查工作区，避免包含无关文件或历史结果目录。

### 9.3 停止条件

出现以下任一情况即按对应阶段完成记录和恢复后停止：

- 场景 1 第 2 轮仍失败。
- 完整测试成功并完成报告。
- 完整测试任意阶段失败并完成报告。
- 集群无法恢复到纠正后基线：停止测试，保留现场并报告，不继续扩大修改范围。

## 10. 本轮明确不做的事项

- 不把当前 512 MiB 等不稳定配置备份为恢复基线。
- 不只修正 Kueue 而跳过 Volcano、Coscheduling 或 YuniKorn 审计。
- 不降级已批准的 Kubernetes 或调度器版本。
- 不在场景 1 第 2 轮失败后继续修复。
- 不在完整测试失败后继续修复或重跑。
- 不在主 Agent 处理首次配置评审后自动循环调用第二轮子 Agent。
- 不在用户确认本文前开始执行。
